<%= simple_form_for(@lumberjack) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :lumberjill %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
