<%= simple_form_for(@lumberjill) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :lumberjack %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
